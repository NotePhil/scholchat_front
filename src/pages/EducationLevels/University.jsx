import React from "react";
import EducationLevel from "./EducationLevel";

const University = ({ theme }) => {
  return <EducationLevel level="university" theme={theme} />;
};

export default University;
